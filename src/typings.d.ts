// so the typescript compiler doesn't complain about the global config object
// import JSON
// declare var config: any;
// declare var config: any;
// const config = JSON.stringify({apiUrl: 'http://localhost:4000'});
export const config = 'http://localhost:4200';
// export config;